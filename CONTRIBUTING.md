# 贡献流程

> 本 lab 的所有改动都走同一条流程。目的是让 git 历史作为教学产物保持可读——所以流程约定不是形式,而是产物质量的一部分。仓库定位与铁律见 [CLAUDE.md](./CLAUDE.md)。

## 硬前提:不碰 main

main 已配置分支保护,直推会被 GitHub 拒绝。一切改动必须:

```
新建分支 → commit → push 分支 → 开 PR → 合入 main
```

## 分支与集成模型

本 lab 走 TBD(主干开发),落地为"短命分支 + PR"。这一节是 CLAUDE.md 铁律 4 的判据来源——铁律讲价值观,判据只在这里权威声明一处。

**短命,用双轨判据界定,满足其一即拆分**:

- **一个 PR 一件事**(主判据,粒度轴):一个分支只承载一个能独立发布的小切片。装不下"一件事"就再切一颗,别攒。这条在 PR review 时判定。
- **分支寿命 ≤ 2 天**(辅判据,时间轴):从建分支到合回不超过两天。这是拦截"一件事被切得太大"的第二道闸——超过两天,分支就有退化成长命分支的风险。

两条是递进关系:先按"一件事"切,时长兜底。不用"改动行数"这类阈值——它对文档/配置类改动误伤大,粒度用语义 + 时间更稳。

**未完成功能怎么合回 main**:TBD 要求每次合入 main 的切片都能独立发布。优先靠"切到足够小、自然可发布"来满足,而不是靠 flag。仅当一个切片确实无法收敛成可发布单元时,才用 feature flag 把未完成路径藏起来再合,形态取最轻量的(环境变量 / 配置项即可),并在引入时记下清理时机。**现在不预先搭建 flag 框架**——按需引入,避免变成没人用的技术债。

## 分支命名

`<类型>/<简短描述>`,用短横线连词。类型取 `feat` / `fix` / `chore` / `docs` 等。例:`chore/init-agent-context`、`feat/user-login`。

## PR 必含项

PR 描述要让审阅者不看代码也能判断"这次在做什么、是否达成"。至少包含:

- **种子四件套**:复述本次任务的意图 / 边界 / 成功标准 / 验证器声明(若源于 `seeds/` 下的种子文件,链接过去即可)。
- **改了什么**:建了/改了哪些文件,各自解决什么问题。
- **验证结论**:验证器跑出的结果(测试通过、类型检查通过等);人工介入的 PR 说明审阅点。

## 合入策略

默认 **squash merge**,让每个 PR 在 main 上收敛成一条可读的历史记录。合入后删除源分支。
