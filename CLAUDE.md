# agent-driven-fe

> 给 AI 的常驻上下文。任何会话进入本仓库,先读这份文件,再动手。它定义"这里是什么、目标函数是什么、哪些铁律不可违反、怎么自驱工作"。操作层面的 PR 流程细节见 [CONTRIBUTING.md](./CONTRIBUTING.md)。

## 这是什么仓库

一个"AI 自驱前端工程"的**样本靶场**。产物不是业务软件,而是一段**可复现的 git 历史**:AI 基于少量种子,自驱地走完需求 → 设计 → 编码 → 调试 → 集成 → 上线,人类只在 PR + comment 处介入。

这个定位改变你的目标函数。产品仓库里你会倾向"少改、稳妥、别留痕迹";这里相反——**git 历史本身就是要展示的产物,过程可见优先于结果干净**。每一轮自驱的 commit 与 PR 轨迹,是"上下文保鲜""PR 往复协作"这些机制的实证。做权衡时,偏向让过程清晰可读。

## 铁律(不可自行更改,违反的代价很高)

1. **绝不直接推 main。** 此刻之后的每一次改动,都走 `新建分支 → commit → 开 PR → 合入 main`。这是本 lab 的第一个验证器。
   > main 已在 GitHub 上配置分支保护:直推会被远端**拒绝**,必须走 PR。这条远端规则由仓库管理员维护,你无权也无需改动它。它是机制层兜底;上面这条纪律是你自己要守的第一层。
2. **只用公开工具链**(GitHub Actions / Sentry / Turborepo 等),不引入任何非公开的内部系统或信息。
3. **仓库是 public 的**,写入的一切都可能被外部看到。不写入任何内部信息、密钥、私有数据。
4. **遵循 TBD(主干开发)。** 短命分支、单切片、频繁合回 main;未完成功能按需用 feature flag 隐藏,不预先铺设 flag 框架。这约束的是改动进入 main 的**粒度与节奏**——铁律 1 只管"必须走 PR",管不到分支能活多久、一次装多少东西,TBD 补的正是这一维。具体判据见 [CONTRIBUTING.md](./CONTRIBUTING.md) 的"分支与集成模型"。

## 怎么自驱工作

驱动单元是**种子四件套**:

- **意图** —— 要做什么。
- **边界** —— 不做什么、技术/范围约束。
- **成功标准** —— 怎样算完成。
- **验证器声明** —— 用什么来客观验证成功标准(测试、类型检查、CI、人工 review 等)。

一个合格种子的字段与格式见 [`seeds/TEMPLATE.md`](./seeds/TEMPLATE.md)。开始任何自驱任务前,先据此把种子写清楚,再展开需求 → 设计 → 编码。人类只在关键节点通过 PR + comment 介入,不逐行盯过程。

种子写好之后"沿什么固定管线推进到上线",由流程引擎蓝图 [`docs/engine-blueprint.md`](./docs/engine-blueprint.md) 定义。任意空会话拿到种子,据此推进,而不是自己发明流程。当前是骨架版本:近期环节可执行,靠后环节为 stub。

## 当前状态

初始化阶段。已建立本上下文(CLAUDE.md / CONTRIBUTING.md / 种子模板)。业务需求、设计资产、代码尚未产出——它们应由 AI 在后续自驱过程中,基于具体种子产出,而非预先搭好。
